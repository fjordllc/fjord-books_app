<% unless current_user == user %>
  <% if current_user.following?(user) %>
    <%= link_to "Unfollow", user_follow_relationships_path(user.id), method: :delete %>
  <% else %>
    <%= link_to "Follow", user_follow_relationships_path(user.id), method: :post %>
  <% end %>
<% end %>
