<% unless current_user == user %>
  <% if current_user.following?(user) %>
    <%= button_to t('.unfollow'), user_follow_relationships_path(user.id), method: :delete %>
  <% else %>
    <%= button_to t('.follow'), user_follow_relationships_path(user.id), method: :post %>
  <% end %>
<% end %>
